# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Swift Image Uploader' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Swift Image Uploader
  pod 'Alamofire', '~> 4.4.0'
  pod 'Cartography', '~> 1.1.0'

  target 'Swift Image UploaderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Swift Image UploaderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
